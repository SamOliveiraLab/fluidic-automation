# Your Pioreactor's Cloudflare Tunnel URL (no trailing slash)
# Get this from: cloudflared tunnel --url http://localhost:80
# Or use a named tunnel for a permanent URL (see README).
VITE_PIOREACTOR_URL=https://your-tunnel-subdomain.trycloudflare.com
